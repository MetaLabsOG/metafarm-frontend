# Distribution

This contract allows to stake token X and receive token X.

It will be useful for META -> META pool.

This is a fork of @metalabsog/farm. The reason is that Reach doesn't allow to store the same token in two different fields,
so we have to create a separate contract for this case.

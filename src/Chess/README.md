# Architecture

Effector is used as a global state store with event-driven architecture.

There are 3 panels: DetailedInfoPanel (left), NavigationPanel (middle), LogPanel (right).

Most logic happens in the middle panel, the left and right "just" show info (i.e. mostly non-interactive)

// Constants for meteor animation
export const DEFAULT_METEOR_COUNT = 10; // Default count for all devices
export const DESKTOP_METEOR_COUNT = 8; // Reduced from 25 to avoid compositor thread pressure
export const MOBILE_METEOR_COUNT = 0; // Reduced from 5 to 3 for mobile devices
export const LOW_END_METEOR_COUNT = 0; // Reduced from 3 to 1 for low-end devices
export const IOS_METEOR_COUNT = 0; // Specific count for iOS devices

// Constants for meteor animation
export const DEFAULT_METEOR_COUNT = 10; // Default count for all devices
export const DESKTOP_METEOR_COUNT = 100; // Higher count for desktop devices
export const MOBILE_METEOR_COUNT = 5; // Lower count for mobile devices
export const LOW_END_METEOR_COUNT = 3; // Minimal count for low-end devices
